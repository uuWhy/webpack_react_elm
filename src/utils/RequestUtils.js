import axios from 'axios';


export default class RequestUtils{
    /**
     *
     * @param {接口} api
     * @param {参数} params
     * @param {请求类型} reqType
     */
    static fetch(api, params, reqType){
        let url = encodeURI(api);

    }
}